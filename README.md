# Indomito Café Website

Static HTML/JS prototype for the Indomito Café landing site.

- `index.html` is the homepage.
- `shared.jsx` contains shared UI and navigation.
- The project uses React + Babel standalone in the browser.

This repo is intended for GitHub Pages or static hosting.
